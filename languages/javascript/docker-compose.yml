services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gl
      POSTGRES_PASSWORD: gl
      POSTGRES_DB: todos
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
